[phases.setup]
nixPkgs = ["python3", "python311Packages.pip", "python311Packages.setuptools"]

[phases.install]
cmds = ["pip install -r requirements-render.txt"]

[start]
cmd = "uvicorn app:app --host 0.0.0.0 --port $PORT"
